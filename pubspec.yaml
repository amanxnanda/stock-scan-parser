name: stock_scan
description: Stock Scan Parser
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: 3.0.5

dependencies:
  bloc: ^8.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.0
  flutter_screenutil: ^5.5.3+2
  intl: ^0.17.0
  stock_scan_domain:
    path: layers/domain
  stock_scan_presentation:
    path: layers/presentation

dev_dependencies:
  bloc_test: ^9.0.3
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  very_good_analysis: ^3.0.1

flutter:
  uses-material-design: true
  generate: true
